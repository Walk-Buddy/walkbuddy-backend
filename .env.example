# cp .env.example .env
#
# ⚠️ 127.0.0.1(TCP)로 붙을 때는 반드시 비밀번호가 있어야 합니다. 비우면 SASL/SCRAM 오류가 납니다.
#
# 한 줄 연결 (설정 시 아래 개별 항목은 무시됨)
# DATABASE_URL=postgresql://사용자:비밀번호@127.0.0.1:5432/swu
#
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=swu
DB_USER=
DB_PASSWORD=여기에_로컬_Postgres_비밀번호

# JWT 등
# JWT_SECRET=
